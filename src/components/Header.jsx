import { AppBar, Toolbar } from '@material-ui/core'
import React from 'react'

function Header() {
    return (
        <AppBar>
            <Toolbar>
                
            </Toolbar>
        </AppBar>
    )
}

export default Header
